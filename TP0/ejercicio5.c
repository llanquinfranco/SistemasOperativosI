#include <stdio.h>

int main() {
    char i;
    unsigned char j;
    printf("El tamaño de char es: %zu bytes\n", sizeof(i));
    printf("El tamaño de unsigned char es: %zu bytes\n", sizeof(j));
    return 0;
}